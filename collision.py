import pygame

"""
print(in_game_Background[player.rect.x // 100 + 1][player.rect.y // 100])
print(in_game_Background[player.rect.x // 100 - 1][player.rect.y // 100])
print(in_game_Background[player.rect.x // 100][player.rect.y // 100 + 1])
print(in_game_Background[player.rect.x // 100][player.rect.y // 100 - 1])

print(player.rect.x // 100, player.rect.y // 100 - 1)
        if in_game_Background[player.rect.x // 100][(player.rect.y + player_size) // 100 - 1].hui():"""